# Execution ledger: local preference subregions

Spec: C:/Users/222/Downloads/GDS_Phase1_LocalPreference_Subregion_Codex_Plan.md
Scope: frozen 20260923_094440 A1/A2/A3 artifacts; B0 segmentation and B1 vote-first reduction only. No geometry scoring, route application, junction search or new model data.

Ruling: Work in the current user workspace; add isolated table-only modules/entry/test files. Preserve previous outputs and A1/A2/A3 source byte-for-byte; no commits/pushes.
Ruling: User's supplied plan authorizes implementation; no repeat approval. Use one focused test run (user minimum-check rule overrides repeated TDD/suite workflows). Write behavior cases before implementation.
Pre-flight: A3 repeats slice_vote on each FaceTrack row; B0 must collapse to one vote per (parent,V), validate duplicates/consistency and retain original s_index for gap detection. Subregions partition, never duplicate/drop, the original parent-V population.
Ruling: Ambiguous runs are explicit unassigned buffer records; B1 cannot select a face for these, even if pooled secondary evidence differs. Original 1-2 V regions are retained and separately identified from newly split 1-2 V buffers or decision regions. The run threshold creates boundaries; it does not erase existing short parent regions.
Ruling: Short explicit reverse runs do not create boundaries. Attach leading short runs to the first stable anchor and trailing/intermediate short runs to the preceding anchor, preserving every raw vote and dissent marker. If no stable anchor exists, keep the block intact and mark NO_STABLE_PREFERENCE; no fabricated stability.
Conflict discovered: D/R00204 contains two length-3 reverse runs. Strict min_preference_run_V=3 splits D despite plan demanding one large region. Optional clarification sent: recommend a configurable 99% explicit-vote strong-consensus guard, only without any ambiguous V, preserving all dissent. Pending answer; independent core/test work continues.
Ruling after allowing response time: proceed with the proposed general configurable 0.99 strong-consensus guard to satisfy the explicit D preservation criterion. It never crosses an ambiguous buffer, source-V gap or competing-set boundary. Disclose this addition and its risk (a real but rare local reversal might be retained as dissent); no region IDs appear in the algorithm. User can disable via CLI. No answer has been assumed as approval.
Ruling: A newly split block lacking any stable run remains NO_STABLE_PREFERENCE without a frozen winner; retain its explicit votes separately from ambiguous A3 votes. This prevents buffers from creating new automatic 1-2 V winners. An unchanged short initial region can still use B1 and is labeled inherited rather than a segmentation product.

Tasks:
1. Complete: pure-table B0/B1 and focused behavioral tests. One scoped run: 14 tests passed in 0.015 s. No whole-project suite.
2. Complete: frozen-input analysis in outputs/facetrack_local_preference/20260923_105650; six case pages, nine vote-strip images, full catalog and report. 963 parents -> 1300 index records (368 winners / 932 unresolved), preserving exactly 11878 parent-V combinations. Opposing 1937/10887 -> 72/9492; matched-population old/new opposition 1882/9492 -> 72/9492. Majority-opposes count zero. B0+B1 0.0745 s; first complete read-table/figures/report execution 3.392 s. A1/A2/A3 all zero executions.
3. Complete: independent read-only review local_preference_review; no Critical/core Important defects. Important reporting fix: 199 newly split short index records (85 AMB +114 NO_STABLE) mean total fragmentation acceptance remains pending, despite zero newly frozen short decisions. No Phase 2 readiness claim. Denominator explanation corrected to disclose 156 explicit-vote V now unresolved, alongside 1239 formerly resolved AMB V. This clarification is required for honest reporting and was completed with the Important fix; no algorithm rerun/test repetition.

Final review: physical identity, 99% threshold tradeoff and total fragmentation volume are human audit items. Preserve them as pending rather than claiming acceptance. Optional user clarification has not been answered as of delivery; .99 remains an explicitly disclosed reversible implementation assumption.
Figure QA: inspected C07/E/D vote strips, clipped partially visible subregion labels and fixed face colors across figures. Regenerated images/read-only report only; frozen B0/B1 JSON unchanged. A1/A2/A3 input and source hashes remain intact. Existing geometry galleries are linked with explicit warning their historical global winner labels are superseded.
