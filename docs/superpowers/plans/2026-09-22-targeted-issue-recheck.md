# 历史局部问题定向复核计划

> **For agentic workers:** 使用 superpowers:executing-plans，在当前任务逐项执行；用户已经要求执行，不额外开启任务或要求再次批准。

**Goal:** 将 E/F/89.95 已验证修复用于历史局部问题的同类案例，汇总成功案例、剩余问题类型和可成立的统计口径。

**Architecture:** 只读两份冻结结果形成逐线来源台账；只对尚未用当前代码验证的实质问题及必要邻线重新识别，已验证成功结果引用原产物。生产算法仅在复现明确根因时做最小修复。

**Tech Stack:** 现有 Python / NumPy / SciPy 生产模块、pickle 原始切片及 Matplotlib 图册。

**Spec:** 当前用户要求：不跑完整结果、不重跑 650 条局部识别，只复核历史存在问题部分；汇总已成功案例并分类剩余路径问题。

## Global Constraints

- 保留 E/F/89.95 当前结果、原始点和面来源，不提交、不推送，不执行全项目测试。
- 无人工真值的诊断计数不得标成识别正确率；人工已确认案例与几何检查、历史成功未重跑结果分别记录。
- 超过一小时的任务预测先排查优化，不扩展为普查。

## Tasks

- [x] 在 `scripts/99_experiments/run_targeted_issue_recheck.py` 中读取历史 650 份冻结结果（不识别），优先复用当前 175 份结果，写出逐线来源、缺失包络、重复分支、超 2 mm 竞争接缝及持续邻线差异。
- [x] 根据 `frozen_inventory.json` 去重、合并同一问题的相邻测线，产生明确的复跑清单与未选原因；已用当前源码计算的结果直接复用。
- [ ] 逐问题窗口调用现有区域准备、P1、联合接缝和 P2；保留代码哈希、计时、失败原因及原始区间验证。只在具体失败有证据时修改对应生产函数并运行针对检查。
- [x] 在独立报告脚本中生成全部 FaceTrack × 五邻线及简洁主轨对照，优先展示剩余问题的不同形态；统一台账列出当前通过、历史成功引用、剩余输入/拓扑/选择/接缝/旁支问题。
- [x] 核验图册、链接、结果来源和统计分母；交付汇总报告、代码改动及已知边界。

**执行停止记录：** 第3项未完成全部110条。完成59条最终结果，18条仅P1，33条未启动。Q07联合搜索性能预筛优化后仍未完成；两次实际运行合计2530.586秒，按当前组耗时外推全清单3936.161秒（65.60分钟），依据用户一小时预测上限要求停止后续识别。报告只使用59条完整新结果及175条既有结果，未将P1缓存标作最终结果。

## Review Focus

- 单侧无主轨是否其实没有合格原始观测，或原曲线已离开显示范围。
- 换轨带内允许的短高差是否被误算为长期来源不一致。
- 来自旧版本的成功记录是否被误标为本轮当前代码通过。
- 多来源/折返是否属于合法原始几何，而非可自动判定的识别错误。
- 同一事件的多个高程采样不得冒充多个独立准确率样本。
